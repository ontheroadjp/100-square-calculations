- docs/L3_implementation/backend/nuts_calc_tex.py.md:9-16 — `nuts_calc_tex.py` compiles with plain `pdflatex` (no CJK/Japanese font package), so any Japanese text in generated PDF output must use ASCII/math workarounds (e.g. `\cdots` instead of the "あまり" label) unless CJK font support is added as separate scope (issue #91)
- docs/L3_implementation/backend/nuts_calc_tex.py.md:247-249 — second recurrence of the same plain-`pdflatex`/no-CJK constraint: the `--with-name-field` label uses English `Name:` instead of the proposed Japanese "なまえ：", tracked for a real fix via LaTeX engine migration (issue #104) (issue #93)
- docs/L3_implementation/frontend/web/src/presetDetail.js.md:29-32 — `frontend/web`'s node:test suites import modules directly (no Vite bundler in between), so a plain CSS import (e.g. `katex/dist/katex.min.css`) placed in a module reachable from `node --test` fails with `ERR_UNKNOWN_FILE_EXTENSION`; such non-JS imports must live in the Vite page-entry file (`preset.js`) instead of the shared module (`presetDetail.js`) it supports (issue #132)
- docs/L3_implementation/backend/problem_generation.py.md:38-40 — data-only generation endpoints must call each renderer's existing pure data-generation functions in-process (no subprocess, no PDF/LaTeX byte output discarded afterward), as the first step toward fully separating the problem-generation (data) layer from the PDF-generation (presentation) layer across `nuts_calc.py`/`nuts_calc_tex.py`; tracked by parent issue #166 and its sub-issues (issue #138)

# Concept

## 目的

このリポジトリは、そろばん式暗算（フラッシュ暗算に近い「4桁変換法」）のトレーニング教材として使う計算ドリルの PDF を生成するツール群である。

根拠:
- `README.md:3` — "This Python script generates math calculation practice printouts in PDF format using the ReportLab library."
- `memo.md:1-543` — 暗算指導法（2桁×1桁、10いくつ同士の掛け算、2桁×11の掛け算などを4桁の数値に変換してから3桁に畳み込む方法）の解説と、学習ステップ（基礎力トレーニング→変換トレーニング→実践→書かないドリル）が詳細に記述されている。
- `factory.sh:79-115` (`_basic` 関数) — `step-01.pdf` 〜 `step-08.pdf` という名前で、`memo.md` の学習ステップ（1桁+1桁の足し算 → 2桁+1桁の足し算 → 九九 → aBc変換 → 2桁×1桁 → 10いくつ同士 → 平方数 → 2桁×11）に対応する PDF を一括生成している。

`memo.md` は実装コードではないが、`100masu.py` が生成する問題形式（`aBc`, `squ`, `99`, `com` など）の教育的な意味づけを説明する一次資料であり、ツールの設計意図を理解する上で必須の根拠である。

## 解決する問題

- 暗算力（特に中学受験を見据えた計算速度）を伸ばすための反復練習用ドリルを、紙に印刷できる形で継続的に・大量に・パラメータを変えながら生成する必要がある（`memo.md:328-394` に計算力の重要性の説明あり）。
- 市販のドリルでは実現しづらい細かいパラメータ調整（桁数、範囲、順序、ページ数、用紙サイズ、答えの有無、A3の4分割/A4横の2分割など）を、スクリプトの引数で制御できるようにしている（根拠: `100masu.py:37-150` の `argparse` 定義）。

## 対象ユーザー

- README や引数のインターフェースは開発者/保護者が CLI を直接操作することを前提としている（根拠: `README.md:18-71` の Usage セクションに `python 100masu.py A4 operations` のような直接実行例のみが記載されており、GUI やインストーラは存在しない）。
- `memo.md` の内容（受験算数、暗算指導のノウハウ）から、想定利用者は小学生（低学年〜中学受験期）とその保護者、または学習塾（`memo.php` 内の "Nuts Education" というブランド名、`100masu.py:674` の copyright 表記 `Copyright(c) 2024 Nuts Education` および `100masu.py:693,698` の `HEADER_STR`/`AUTHOR` = `'Nuts Education'`）であると推定される。ただし、"Nuts Education" が具体的にどの事業体かは本リポジトリ内から確認できない（未確認）。

## 設計上の制約

- 出力は PDF 1本（または問題用とマージなしの解答用の2本）であり、Web サーバーやデータベースは存在しない。CLI → ReportLab → PDF/CSV のワンショット変換のみ（根拠: `100masu.py:678-1219` の `main()` 関数が唯一の実行フロー）。
- 用紙サイズは A3 / A4 / A4横(landscape) / B5 の4種類に限定され、それぞれ余白・フォントサイズ・仮想ページ分割数（A3は4分割、A4横は2分割）が異なるレイアウトロジックがハードコードされている（根拠: `100masu.py:702-796`）。
- 問題タイプ（`command` 引数）は `ope`（四則演算）, `com`（補数）, `100`（100マス計算）, `99`（九九）, `aBc`（4桁→3桁変換トレーニング）, `squ`（平方数）, `pi`（円周率倍）の7種類に限定される（根拠: `100masu.py:51-56` の `choices`）。

## 未確認事項

- "Nuts Education" が何を指す組織/屋号か: リポジトリ内に説明なし。確認するには外部情報（README や別リポジトリ、事業者情報）が必要。
- ライセンス: `LICENSE` ファイルはリポジトリ内に存在しない（`find -iname "LICENSE*"` で該当なし）。再配布・利用条件は未確認。

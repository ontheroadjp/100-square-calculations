# `backend/app.py`

## 目的・役割

`nuts_calc_tex.py` を Web 経由で呼び出す Flask API。エンドポイントは `POST /generate-pdf`、`POST /generate-problems`(issue #138)、`GET /renderer-info`(issue #46)の3つ。JSON/HTTP 境界を本ファイルが担い、未移行 PDF コマンド・variant の CLI/subprocess 実行は `backend/renderers.py`、問題データのみのプロセス内生成は `backend/problem_generation.py` に委譲する。`POST /generate-pdf` の内部プレゼンテーション API 経路は `com`(#199)、`ope` の plain/tree/flat multi-term(#205〜#207)、`99`(#208)、`squ`(#209)、`pi`(#210)、`lcm`(#211)、`gcd`(#212)、`aBc`(#213)、`evenodd`(#214)、`multiples`(#215)、`divisors`(#216)、`frac`(#217)、基本2項 `mixed`(#218)、`divfrac`(#219)、`simplify`(#220)、`frac2dec`(#221)、`dec2frac`(#222)、`commondenom`(#225、コンテンツフォーマットパターン4c)、`100`(#229、単一・番号なしの Layer-2 variant + `grid_layout='block'`)、`ope --missing-value`(#223、コンテンツフォーマットパターン2)、`compare`(#224、コンテンツフォーマットパターン3)、`ope --vertical`(#227、コンテンツフォーマットパターン6、多行の xlop/longdivision 出力のため `grid_layout='tabular'`)、`ope --intermediate`(#226、コンテンツフォーマットパターン5)へ段階的に展開済みである。未移行は `mixed` の拡張 variant のみ。`POST /generate-problems` は issue #228 で `command_type == '100'`(100マス加算表)にも対応し、他コマンドの `{"problems": [...]}` とは別の単一テーブル用 envelope `{"table": {...}}` を返す(#169 の対象外判断を反転)。

## 動作の概要

`command_type == 'divfrac'`/`'simplify'`/`'frac2dec'`/`'dec2frac'`/`'compare'`/`'commondenom'` は `_generate_divfrac_pdf`/`_generate_simplify_pdf`/`_generate_frac2dec_pdf`/`_generate_dec2frac_pdf`/`_generate_compare_pdf`/`_generate_commondenom_pdf` を直接呼び、基本2項 `mixed` は `_is_plain_mixed_pdf_request` を通過した場合だけ `_generate_mixed_pdf` を呼ぶ。これらは内部プレゼンテーション API で生成するため subprocess fallback の対象ではない(`app.py:90-187,265-327,1264-1658`)。

- `POST /generate-pdf`(`app.py:1717-1801`): `paper_size`/`command_type` を検証し、`com`/`lcm`/`divfrac`/`gcd`/`evenodd`/`99`/`aBc`/`pi`/`100`/移行済み `ope` variants(plain 2-term / tree / flat multi-term / `--missing-value` / `--intermediate`)/`squ`/`multiples`/`divisors`/`frac`/`simplify`/`frac2dec`/`dec2frac`/`compare`/`commondenom` と基本2項 `mixed` は対応する `_generate_*_pdf` で内部プレゼンテーション API を呼ぶ。それ以外は `renderers.run(data, PDF_OUTPUT_DIR, renderer_name)` の subprocess 経路へフォールバックする。成功時は `send_file` で PDF を返し、入力・コンパイル・subprocess・ファイルの各例外は既存の JSON 500 応答へ変換する。
  - `_generate_com_pdf(data, output_dir)`(`app.py:22-86`、issue #199): `com` 専用のベーシックケースのみ対応する PDF ビルダー。`a_value` の検証(必須、`nuts_calc_tex.MIN_COMPLEMENT_TARGET` 以上)は `problem_generation.validate_com_target(data.get('a_value'))` へ委譲する(issue #237 以前は本関数がインラインで重複実装していたが、`backend/problem_generation.py`([[problem_generation.py]] 参照)の `_generate_com_problems` と共有する形に統一した)。`rows`/`columns`(既定 `nuts_calc_tex.DEFAULT_ROWS`=10/2、CLI 既定と一致)を検証したうえで、`nuts_calc_tex.generate_com_problems` → `PresentationPage` → `build_presentation_document_tex(content_format=build_com_slot_content_tex, page_shell=DEFAULT_PAGE_SHELL, content_area_layout=ContentAreaLayout(rows, columns), show_answer=False)` の順に呼び、`nuts_calc_tex.get_latex_engine_adapter()` で解決したエンジンの `compile()` を直接呼んで PDF ファイルを書き出す。常に1ページ・blank(練習用)版のみを返す — `with_bottom_answer`/`with_name_field`/複数ページ/`merge` は未対応(issue #199 のスコープ外、issue 本文に明記)。
  - `_generate_com_pdf` は `data.get('a_value')` を補数ターゲット(値そのもの)として直接読む。`com` は `nuts_calc_tex.DIGIT_COUNT_SHORTHAND_COMMANDS` に含まれないため、これは issue #230 の再設計後も無変更で正しい。
  - `_generate_lcm_pdf(data, output_dir)`(issue #211): `lcm` 専用のベーシックケースのみ対応する PDF ビルダー。`_generate_com_pdf` と同じ構造だが、`lcm` は `ope`/`100`/`gcd`/`divfrac` と同じく `nuts_calc_tex.DIGIT_COUNT_SHORTHAND_COMMANDS` に含まれるため `a`/`b` の範囲は `_generate_ope_pdf` と同じく `problem_generation.resolve_digit_count_range(data, 'a_digits', 'a_min', 'a_max', ...)`(`b` も同様)経由で解決する。ルーティング判定用の `_is_lcm_pdf_request` ヘルパーは存在しない — `lcm` は `vertical`/`intermediate`/`use_parentheses`/`missing_value`/`terms`系などの variant フラグを一切持たない(`nuts_calc_tex.py` の `_init()` が `command != 'ope'` を理由に無条件で拒否)ため、`generate_pdf()` 内で `com` と同じ単純な `command_type == 'lcm'` の等価判定だけでルーティングできる。`nuts_calc_tex.generate_number_pair_problems(math.lcm, nums_a, nums_b, rows * columns, 1)` → `PresentationPage` → `build_presentation_document_tex(content_format=nuts_calc_tex.build_lcm_slot_content_tex)` の順に呼ぶ。`rows`/`columns` の既定・検証・常に1ページ・blank 版のみという制約は `_generate_com_pdf`/`_generate_ope_pdf` と同一。
  - `_generate_divfrac_pdf(data, output_dir)`(`app.py:265-327`、issue #219): `divfrac` の基本ケースを内部 API で生成する。`DIGIT_COUNT_SHORTHAND_COMMANDS` の契約どおり `a_digits`/`b_digits` または明示 range を `problem_generation.resolve_digit_count_range` で解決し、0除算を避けるため `b_min >= 1` を検証する。`generate_divfrac_problems` → `PresentationPage` → `build_presentation_document_tex(content_format=build_divfrac_slot_content_tex)` で、未約分の `a/b` 表示を保つ1ページ blank PDFを作る。
  - `_generate_gcd_pdf(data, output_dir)`(`app.py:328-393`、issue #212): `_generate_lcm_pdf` と同じ桁数レンジ解決・レイアウト検証・1ページ blank 版という基本ケース限定の構造を使い、生成関数だけを `generate_number_pair_problems(math.gcd, ...)`、Layer-3 content だけを `build_gcd_slot_content_tex` に差し替える。`command_type == 'gcd'` の完全一致でのみこの経路へ入り、CLI と他コマンドの subprocess fallback は変更しない。
  - `_generate_evenodd_pdf(data, output_dir)`(`app.py:394-446`、issue #214): `a_min`/`a_max`(既定は `problem_generation.DEFAULT_A_MIN`/`DEFAULT_A_MAX`)から `generate_evenodd_problems` で `rows * columns` 問を生成し、`build_evenodd_slot_content_tex` を Layer-3 content として1ページの blank PDFを構成する。`command_type == 'evenodd'` の完全一致で内部 API へ入り、`even`/`odd` の表示と既存 blank 表示を変えない。回答版・名前欄・複数ページ・merge はこの builder には未配線である。
  - `_generate_kuku_pdf(data, output_dir)`(issue #208): `99`(九九)専用の PDF ビルダー。`_generate_com_pdf` と同じ構造で、`a_value` の検証(必須)は `problem_generation.validate_kuku_a_value(data.get('a_value'))` へ委譲する(`validate_com_target` の抽出パターンをそのまま踏襲)。`_generate_com_pdf` と異なり `descend`/`shuffle` も `data` から読んで `nuts_calc_tex.generate_kuku_problems(a_value, rows*columns, 1, descend, shuffle)` へそのまま渡す — `com` の他の未対応フラグと違い、`frontend/web` の `g2-kuku` プリセット(出題順序設定)が実際にこれらを送信する生きた経路があるため、無視すると回帰になる。`rows`/`columns` の既定・検証は `_generate_com_pdf` と同一。`content_format=nuts_calc_tex.build_kuku_slot_content_tex` を使う(常に非-reverse の `$a \times b = c$` 形。`reverse` は未対応)。`with_bottom_answer`/`with_name_field`/複数ページ/`merge` は `_generate_com_pdf` と同様に未対応。
  - `_generate_abc_pdf(data, output_dir)`(`app.py:517-567`、issue #213): `aBc` 専用のベーシックケース PDF ビルダー。`aBc` は入力値を持たないため、`rows`/`columns` の既定値と1以上の検証だけを行い、`generate_abc_problems(rows*columns, 1)` → `PresentationPage` → `build_presentation_document_tex(content_format=build_abc_slot_content_tex, show_answer=False)` の順に呼ぶ。常に1ページ・blank版で、`with_bottom_answer`/`with_name_field`/複数ページ/`merge` は未対応。エンジンの `SystemExit` は他の内部 API builder と同じく stdout を捕捉して `RuntimeError` に変換する。
  - `_generate_pi_pdf(data, output_dir)`(issue #210): `pi` 専用のベーシックケースのみ対応する PDF ビルダー。`_generate_com_pdf` と同じ構造で、`a_value`(起点となる整数)を `problem_generation.validate_pi_start(data.get('a_value'))` へ委譲して検証する(`com` の `validate_com_target` と対になる共有バリデータ、issue #237 の規約に従い最初から `problem_generation.py` 側に抽出)。`pi` は `com` と同じく `nuts_calc_tex.DIGIT_COUNT_SHORTHAND_COMMANDS` に含まれないため `a_value` を直接読む。`descend`/`shuffle` は `bool(data.get(..., False))` で読み、`nuts_calc_tex.generate_pi_problems(start_num, rows*columns, 1, descend, shuffle)` → `PresentationPage` → `build_presentation_document_tex(content_format=nuts_calc_tex.build_pi_slot_content_tex)` の順に呼ぶ。`rows`/`columns` の既定・検証・常に1ページ・blank 版のみという制約、`reverse`(式の左右反転)/`with_bottom_answer`/`with_name_field`/複数ページ/`merge` が未対応な点は `_generate_com_pdf` と同一(issue #210 のスコープ外)。
  - `_generate_hundred_square_pdf(data, output_dir)`(`app.py:1426-1480`、issue #229): `100`(100マス加算表)を内部プレゼンテーション API で生成する。他の全 `_generate_*_pdf` と異なり `100` は1ページ1表・問題番号なしのため、Layer 2 の第2 variant `ContentAreaLayout(rows=1, columns=1, numbered=False)`(単一・番号なし・全 content-area スロット)と `grid_layout='block'` を使い、legacy の `build_hundred_square_pages()` の `Page(columns=1, layout='block')` と等価な合成にする。Layer-3 content format は `nuts_calc_tex.build_hundred_square_slot_content_tex`(既存 `build_hundred_square_block_tex` をそのまま移植、ガイドライン doc 準拠のマクロ化は #185/#270)。a/b 軸レンジの解決と最小値(5)チェックは `problem_generation.resolve_hundred_square_axes` へ委譲し、`POST /generate-problems` の `100` 経路(issue #228)と同一ロジックを共有する。`_generate_com_pdf` 系と同じく常に1ページ・blank 版のみ(`show_answer`/`merge`/複数ページ/`page` は未配線)。`command_type == '100'` の完全一致でこの経路へ入る。生成 TeX が legacy `build_document_tex` 経路とバイト等価になることを `backend/tests/test_web_backend_app.py` の `test_generate_pdf_hundred_square_matches_legacy_document_output` で担保。
  - `_is_plain_ope_pdf_request(data)`(issue #205): ルーティング判定専用の述語。`command_type == 'ope'` かつ `vertical`/`intermediate`(それぞれ別コンテンツフォーマットのパターン6/5)、`use_parentheses`(tree variant。issue #206 で `_is_tree_ope_pdf_request` に切り出して別途移行済み)、`missing_value`(パターン2。issue #223 で `_is_missing_value_ope_pdf_request` に切り出して別途移行済み)、`terms`/`terms_min`/`terms_max`/`mixed_operators`(flat multi-term variant。issue #207 で `_is_multi_term_ope_pdf_request` に切り出して別途移行済み)のいずれも指定されていない場合のみ真を返す。真の場合だけ `_generate_ope_pdf` へルーティングし、それ以外(variant フラグが1つでも立っている場合)は他の `command_type` と同じ subprocess 経路にフォールバックする。
  - `_generate_ope_pdf(data, output_dir)`(issue #205): plain 2-term `ope` 専用の PDF ビルダー。`_generate_com_pdf` と同じ構造だが、`ope` は `nuts_calc_tex.DIGIT_COUNT_SHORTHAND_COMMANDS` に含まれるため `a`/`b` の範囲は `problem_generation.resolve_digit_count_range(data, 'a_digits', 'a_min', 'a_max', ...)`(`b` も同様)経由で解決する(`_generate_com_pdf` の docstring がこの区別を明記していた設計判断どおり)。`operator`(既定 `problem_generation.DEFAULT_OPERATOR` = `['add']`)・`a_decimal_places`/`b_decimal_places`・`carry_mode`/`remainder_mode`/`result_max` をそのまま `nuts_calc_tex.generate_ope_problems` へ渡し、`content_format=nuts_calc_tex.build_ope_slot_content_tex`(#184 のパターンを踏襲した番号なし Layer-3 content、`nuts_calc_tex.py` 側で新設)を使って `build_presentation_document_tex` を呼ぶ。`rows`/`columns` の既定・検証・常に1ページ・blank 版のみという制約は `_generate_com_pdf` と同一。
  - `_is_tree_ope_pdf_request(data)`(issue #206): ルーティング判定専用の述語。`command_type == 'ope'` かつ `use_parentheses` が真で、かつ `vertical`/`intermediate`/`missing_value` のいずれも指定されていない場合のみ真を返す(いずれも `nuts_calc_tex.py` の `_init()` バリデーションで `--use-parentheses` と併用不可、`nuts_calc_tex.py:676-692`)。`terms`/`terms_min`/`terms_max`/`mixed_operators` は `--use-parentheses` 自身の N項一般化(issue #71)であり、`_is_plain_ope_pdf_request` とは異なりこれらが指定されていても真を返す(tree variant として内部 API へルーティングする)。
  - `_generate_tree_ope_pdf(data, output_dir)`(issue #206): `ope --use-parentheses`(tree variant)専用の PDF ビルダー。`a`/`b` レンジは `_generate_ope_pdf` と同じく `problem_generation.resolve_digit_count_range` 経由で解決する。`terms`/`terms_min`/`terms_max` は `problem_generation._determine_ope_variant` の tree 分岐と同じロジック(`terms` が指定されていれば `terms_min`/`terms_max` を上書き、`nuts_calc_tex.resolve_term_range(..., use_parentheses=True)` でフロア(`TERM_COUNT_FLOOR_PARENTHESES`=3)にクランプ)をこの関数内で再実装している(`_determine_ope_variant` 自体は missing_value/multi_term 分岐も持つため直接は呼ばない)。`nuts_calc_tex.generate_tree_ope_problems` → `PresentationPage` → `build_presentation_document_tex(content_format=nuts_calc_tex.build_tree_ope_slot_content_tex)` の順に呼ぶ。`rows`/`columns` の既定・検証・常に1ページ・blank 版のみという制約は `_generate_com_pdf`/`_generate_ope_pdf` と同一。
  - `_is_multi_term_ope_pdf_request(data)`(issue #207): ルーティング判定専用の述語。`command_type == 'ope'` かつ `vertical`/`intermediate`/`use_parentheses`/`missing_value` のいずれも指定されておらず、かつ `mixed_operators` が真、または `terms`/`terms_min`/`terms_max` のいずれかが指定されている場合のみ真を返す。`nuts_calc_tex.py` の `_ope_uses_multi_term(ini)` 述語(`nuts_calc_tex.py:3091-3097`)と同じ条件をルーティング層で先取りしている。
  - `_generate_multi_term_ope_pdf(data, output_dir)`(issue #207): かっこなし flat multi-term `ope` 専用の PDF ビルダー。`a`/`b` レンジは `_generate_ope_pdf`/`_generate_tree_ope_pdf` と同じく `problem_generation.resolve_digit_count_range` 経由で解決する。`terms`/`terms_min`/`terms_max` の解決は `_generate_tree_ope_pdf` と同型だが、`nuts_calc_tex.resolve_term_range(..., use_parentheses=False)` を呼ぶ点が異なる(フロアは `TERM_COUNT_FLOOR_DEFAULT`=2)。`nuts_calc_tex.generate_multi_term_ope_problems` → `PresentationPage` → `build_presentation_document_tex(content_format=nuts_calc_tex.build_multi_term_ope_slot_content_tex)` の順に呼ぶ。`rows`/`columns` の既定・検証・常に1ページ・blank 版のみという制約は他の `_generate_*_pdf` と同一。これで #201(パターン1a移行トラッキングissue)の `ope` バリアント(plain 2-term/tree/multi-term)が全て内部プレゼンテーション API 経路に移行した。
  - `_is_missing_value_ope_pdf_request(data)`(issue #223): ルーティング判定専用の述語。`command_type == 'ope'` かつ `missing_value` が真で、かつ `vertical`/`intermediate`/`use_parentheses`/`mixed_operators`/`terms`/`terms_min`/`terms_max` のいずれも指定されていない場合のみ真を返す(いずれも `nuts_calc_tex.py` の `_init()` バリデーションで `--missing-value` と併用不可、`nuts_calc_tex.py:684-715`)。真の場合だけ `_generate_missing_value_ope_pdf` へルーティングし、無効な併用(例: `use_parentheses` + `missing_value`)は他の `command_type` と同じ subprocess 経路にフォールバックする。
  - `_generate_missing_value_ope_pdf(data, output_dir)`(issue #223): `ope --missing-value`(虫食い算 / mushikuizan、コンテンツフォーマットパターン2)専用の PDF ビルダー。`_generate_ope_pdf` と同じ構造で、`a`/`b` レンジは `problem_generation.resolve_digit_count_range` 経由、`operator`(既定 `problem_generation.DEFAULT_OPERATOR` = `['add']`)・`result_max`・`rows`/`columns` を解決する。`_generate_ope_pdf` と異なり `a_decimal_places`/`b_decimal_places`/`carry_mode`/`remainder_mode` は渡さない — `nuts_calc_tex.generate_missing_value_problems(nums_a, nums_b, operator, rows*columns, 1, result_max)` はこれらの引数を取らず、虫食い算は整数限定のため(`MissingValueProblem` の docstring 参照)。`content_format=nuts_calc_tex.build_missing_value_slot_content_tex`(#184 のパターンを踏襲した番号なし Layer-3 content、`nuts_calc_tex.py` 側で新設)を使って `build_presentation_document_tex(content_area_layout=ContentAreaLayout(rows, columns), show_answer=False)` を呼ぶ。`rows`/`columns` の既定・検証・常に1ページ・blank 版のみという制約は他の `_generate_*_pdf` と同一。
  - `_is_vertical_ope_pdf_request(data)`(issue #227): ルーティング判定専用の述語。`command_type == 'ope'` かつ `vertical` が真で、かつ `intermediate`/`use_parentheses`/`missing_value`/`mixed_operators`/`terms`/`terms_min`/`terms_max` のいずれも指定されていない場合のみ真を返す(いずれも `nuts_calc_tex.py` の `_init()` バリデーションで `--vertical` と併用不可、`nuts_calc_tex.py:754-795`)。`_is_plain_ope_pdf_request` と同型で、`vertical` を「拒否」ではなく「必須」にしただけ。真の場合だけ `_generate_vertical_ope_pdf` へルーティングし、無効な併用(例: `use_parentheses` + `vertical`)は他の `command_type` と同じ subprocess 経路にフォールバックする。
  - `_generate_vertical_ope_pdf(data, output_dir)`(issue #227): `ope --vertical`(筆算 / hissan、コンテンツフォーマットパターン6)専用の PDF ビルダー。`_generate_ope_pdf` と同じ構造で、`a`/`b` レンジは `problem_generation.resolve_digit_count_range` 経由、`operator`・`a_decimal_places`/`b_decimal_places`・`carry_mode`/`remainder_mode`・`result_max`・`rows`/`columns` を解決し `nuts_calc_tex.generate_ope_problems` へ渡す(`--vertical` は2項 add/sub/mul/div なので carry/remainder も有効)。`_generate_ope_pdf` と異なる点は2つ: (1) 多行の xlop(add/sub/mul)/longdivision(div)出力のため `build_presentation_document_tex(..., grid_layout='tabular')`(legacy CLI の `build_ope_page_pair` の `layout='tabular'` と同じグリッド)を指定する。(2) `div` オペレーターかつ除数が小数(`b_decimal_places > MIN_DECIMAL_PLACES`)の場合、`longdivision` の `\intlongdivision` が整数除数しか取らないため `nuts_calc_tex.py` の `_init()`(`nuts_calc_tex.py:900-912`)と同一メッセージの `ValueError` を送出して HTTP 500 にする — app.py は `_init()` を経由しないためこのチェックを再実装している(`--vertical` + 桁数一致の小数併用は issue #134 で許容)。`content_format=nuts_calc_tex.build_vertical_ope_slot_content_tex`(#227 で `nuts_calc_tex.py` 側に新設した番号なし Layer-3 content、`build_vertical_calc_tex` への薄い委譲)を使い `build_presentation_document_tex(content_area_layout=ContentAreaLayout(rows, columns), show_answer=False)` を呼ぶ。`rows`/`columns` の既定・検証・常に1ページ・blank 版のみという制約は他の `_generate_*_pdf` と同一。
  - `_is_intermediate_ope_pdf_request(data)`(issue #226): ルーティング判定専用の述語。`command_type == 'ope'` かつ `intermediate` が真で、かつ `vertical`/`use_parentheses`/`missing_value`/`mixed_operators`/`terms`/`terms_min`/`terms_max` のいずれも指定されていない場合のみ真を返す(いずれも `nuts_calc_tex.py` の `_init()` バリデーションで `--intermediate` と併用不可、`nuts_calc_tex.py:750-797`)。真の場合だけ `_generate_intermediate_ope_pdf` へルーティングし、無効な併用(例: `use_parentheses` + `intermediate`)は他の `command_type` と同じ subprocess 経路にフォールバックする。
  - `_generate_intermediate_ope_pdf(data, output_dir)`(issue #226): `ope --intermediate`(段階的暗算チェーン、コンテンツフォーマットパターン5、#174/B-5 の breadth=1 pattern-5 batch)専用の PDF ビルダー。`_generate_ope_pdf` と同じ構造で、`a`/`b` レンジは `problem_generation.resolve_digit_count_range` 経由、`result_max`・`rows`/`columns` を解決する。`--intermediate` は単一 `mul` 演算子・単一桁の第2オペランドしか許さず、小数桁も拒否する(`nuts_calc_tex.py:750-797,880-881`)ため、`operator`(既定 `['mul']`)が `['mul']` でない場合と `b_max > nuts_calc_tex.INTERMEDIATE_SINGLE_DIGIT_MAX`(9)の場合は subprocess 経路と同じ文言の `ValueError` を送出する(スコープ外リクエストがサイレントに別のワークシートを生成しないように)。`_generate_missing_value_ope_pdf` と同じく `a_decimal_places`/`b_decimal_places`/`carry_mode`/`remainder_mode` は渡さない — `mul` 限定 variant では無意味なため。`nuts_calc_tex.generate_ope_problems(nums_a, nums_b, ['mul'], rows*columns, 1, result_max=...)` → `PresentationPage` → `build_presentation_document_tex(content_format=nuts_calc_tex.build_intermediate_ope_slot_content_tex, content_area_layout=ContentAreaLayout(rows, columns), show_answer=False)` の順に呼ぶ。番号なし slot formatter `build_intermediate_ope_slot_content_tex` は issue #226 で `nuts_calc_tex.py` 側に新設(#268 が retrofit 済みの `build_horizontal_intermediate_block_tex` から問題番号を分離、block builder はこれへ委譲する形に変わった)。`rows`/`columns` の既定・検証・常に1ページ・blank 版のみという制約は他の `_generate_*_pdf` と同一。
  - `_generate_squ_pdf(data, output_dir)`(issue #209): `squ` 専用のベーシックケースのみ対応する PDF ビルダー。`_generate_com_pdf` と同じ構造・同じスコープ制約(常に1ページ・blank 版のみ、`descend`/`shuffle`/`reverse`/`with_bottom_answer`/`with_name_field`/複数ページ/`merge` は未対応)。`a_value` の検証(必須のみ、`com` と異なり下限チェックなし)は `problem_generation.validate_squ_start(data.get('a_value'))` へ委譲する(`validate_com_target`/issue #237 のデデュープパターンを踏襲)。`squ` は `nuts_calc_tex.DIGIT_COUNT_SHORTHAND_COMMANDS` に含まれないため `a_value` を開始する二乗数の起点として直接読む(`com` と同じ理由)。`nuts_calc_tex.generate_squ_problems(start_num, rows*columns, 1, False, False)`(`descend`/`shuffle` は常に `False`)→ `PresentationPage` → `build_presentation_document_tex(content_format=build_squ_slot_content_tex, ...)` の順に呼ぶ。
  - `_generate_multiples_pdf(data, output_dir)`(`app.py:1036-1099`、issue #215): `a_min`/`a_max` と `multiples_count` を読み、`a_min >= 1`、`multiples_count >= MIN_MULTIPLES_COUNT`、rows/columns の下限を検証する。`generate_multiples_problems` と `build_multiples_slot_content_tex` を合成して1ページの blank PDFを生成し、指定された `multiples_count` を維持する。回答版・名前欄・複数ページ・merge はこの builder には未配線である。
  - `_generate_divisors_pdf(data, output_dir)`(`app.py:1100-1153`、issue #216): `a_min`/`a_max` と rows/columns を検証し、`generate_divisors_problems` と番号なし `build_divisors_slot_content_tex` を合成して1ページの blank PDFを生成する。可変長の約数リストと `BLANK_ANSWER_TEX` は既存 block formatter と同じである。
  - `_generate_frac_pdf(data, output_dir)`(`app.py:1154-1263`、issue #217): 分子・分母桁数、分母条件、真分数条件、operator、帯分数 form、reducible mode、rows/columns を Flask プロセス内で検証し、`generate_fraction_problems` と `build_fraction_slot_content_tex` を合成する。常に1ページの blank PDFで、既存の `\displaystyle`・厳密分数・帯分数表示を維持する。
  - `_generate_simplify_pdf(data, output_dir)`(`app.py:1264-1326`、issue #220): `numerator_digits`/`denominator_digits` を `MIN_FRACTION_DIGITS`〜`MAX_FRACTION_DIGITS` に制限し、rows/columns を検証する。`generate_simplify_problems` と pattern-4b の `build_simplify_slot_content_tex` を合成し、未約分の入力から約分結果へ向かう既存 `\Rightarrow` 本文を保った1ページ blank PDFを生成する。
  - `_generate_frac2dec_pdf(data, output_dir)`(`app.py:1321-1376`、issue #221): `numerator_digits`/`denominator_digits` を `MIN_FRACTION_DIGITS`〜`MAX_FRACTION_DIGITS` に制限し、rows/columns を検証する。`generate_frac2dec_problems` と pattern-4b の `build_frac2dec_slot_content_tex` を合成し、分数から有限小数へ向かう既存 `\Rightarrow` 本文を保った1ページ blank PDFを生成する。`command_type == 'frac2dec'` の完全一致だけがこの経路へ入り、他の未移行 command は subprocess fallback を維持する。
  - `_generate_dec2frac_pdf(data, output_dir)`(`app.py:1378-1424`、issue #222): `frac2dec` と対になる小数→分数変換の内部 API builder。`frac2dec` と異なり `numerator_digits`/`denominator_digits` を受け取らない — `generate_dec2frac_problems(order, start_index)` は digit 引数を取らず、小数桁数は `nuts_calc_tex.py` 側の固定レンジ(`DEC2FRAC_MIN_DECIMAL_PLACES`〜`MAX_DECIMAL_PLACES`)からランダムに決まるため、検証は `rows`/`columns`(既定 `nuts_calc_tex.DEFAULT_ROWS`=10/2)の下限だけ。`generate_dec2frac_problems(rows * columns, 1)` と pattern-4b の `build_dec2frac_slot_content_tex` を合成し、小数から約分済み分数へ向かう既存 `\Rightarrow` 本文を保った1ページ blank PDFを生成する。`command_type == 'dec2frac'` の完全一致だけがこの経路へ入り、他の未移行 command は subprocess fallback を維持する。エンジンの `SystemExit` は他の内部 API builder と同じく stdout 捕捉 → `RuntimeError` へ変換する。
  - `_generate_compare_pdf(data, output_dir)`(issue #224、`docs/latex/tex_content_format_taxonomy.md` のコンテンツフォーマットパターン3、#174/B-5 の breadth=1 batch): 分数比較(`a` と `b` の大小)の内部 API builder。`compare` は `DIGIT_COUNT_SHORTHAND_COMMANDS` に含まれないため `_generate_simplify_pdf`/`_generate_frac2dec_pdf` と同じく `numerator_digits`/`denominator_digits` を `data` から直接読み `MIN_FRACTION_DIGITS`〜`MAX_FRACTION_DIGITS` に制限し、`rows`/`columns`(既定 `nuts_calc_tex.DEFAULT_ROWS`=10/2)の下限を検証する。比較 pattern・分数 form・operand kind は CLI 既定(`different-denominators` / `proper` / `proper` / 分数vs分数)固定で `generate_fraction_comparison_problems('different-denominators', 'proper', 'proper', numerator_digits, denominator_digits, rows * columns, 1)` を呼び、pattern-3 の番号なし `build_fraction_comparison_slot_content_tex`(`nuts_calc_tex.py` 側で新設、既存 `build_fraction_comparison_block_tex` から問題番号を分離)を `content_format` として合成する。blank 版は関係記号の位置を共有マクロ `\boxedblank`(`COMPARE_REL_BLANK_TEX`、`com`/`missing-value` と共有の角枠。issue #266 以前は定数 `BOXED_BLANK_TEX`)で隠す。`command_type == 'compare'` の完全一致だけがこの経路へ入り、`comparison_pattern`/`a_fraction_form`/`b_fraction_form`/`a_kind`/`b_kind` などの非既定オプションは basic-case scope 外のため転送しない(#199/#222 と同じ制約)。`with_bottom_answer`/`with_name_field`/複数ページ/merge/`show_answer=True` も未対応。エンジンの `SystemExit` は他の内部 API builder と同じく stdout 捕捉 → `RuntimeError` へ変換する。
  - `_generate_commondenom_pdf(data, output_dir)`(`app.py:1603-1658`、issue #225): `commondenom`(通分)の基本ケースを内部 API で生成する。`_generate_simplify_pdf` と同型で、`numerator_digits`/`denominator_digits` を `MIN_FRACTION_DIGITS`〜`MAX_FRACTION_DIGITS`(1〜3)に制限し、rows/columns の下限を検証する(`commondenom` は `DIGIT_COUNT_SHORTHAND_COMMANDS` に含まれないため `problem_generation.resolve_digit_count_range` は使わず `data.get(...)` を直接読む)。`generate_commondenom_problems(numerator_digits, denominator_digits, rows * columns, 1)` と pattern-4c の番号なし `build_commondenom_slot_content_tex` を合成し、2つの元分数から `\Rightarrow` を経て通分後の2分数へ向かう既存本文を保った1ページ blank PDFを生成する。`command_type == 'commondenom'` の完全一致だけがこの経路へ入り、他の未移行 command は subprocess fallback を維持する。エンジンの `SystemExit` は他の内部 API builder と同じく stdout 捕捉 → `RuntimeError` へ変換する。`with_bottom_answer`/`with_name_field`/複数ページ/`merge` は未配線。
  - `_is_plain_mixed_pdf_request`/`_generate_mixed_pdf`(`app.py:90-187`、issue #218): `terms` 系、`mixed_operators`、`reducible_mode` のない基本2項 `mixed` だけを内部 API に振り分ける。fraction digit、decimal places、operand kind、operator、rows/columns を検証し、`generate_mixed_problems` と `build_mixed_slot_content_tex` を合成する。除外した拡張 variant は従来の subprocess 経路を維持する。
- `engine_adapter.compile()` はコンパイル失敗時に例外を raise せず `nuts_calc_tex.failure()`(`print()` + `exit(1)` = `SystemExit`)を呼ぶ設計で、これは元々 CLI の subprocess 隔離を前提にしている(`LatexEngineAdapter.compile` の docstring 参照)。各 `_generate_*_pdf` helper(`divfrac`/`simplify`/`frac2dec`/`commondenom` を含む)は `compile()` を `contextlib.redirect_stdout` で囲み、`failure()` の出力を捕捉して `SystemExit` を `RuntimeError(f'PDF generation failed: {captured}')` に変換する。ルート側の `except RuntimeError` 節がこれを HTTP 500 に変換する。
- `POST /generate-problems`(issue #138、`app.py:1507-1538`): 必須パラメータ(`paper_size`/`command_type`/`num`、`num` は正の整数)を本ファイルで検証する(`app.py`の`generate_problems`ハンドラ)。`renderers.get_renderer_name()` でレンダラーを解決したうえで、`command_type == '100'`(issue #228)の場合だけ `problem_generation.generate_hundred_square_table(data)` を呼び、その戻り値(`{'table': {...}}`)をそのまま `jsonify` して返す。それ以外は `problem_generation.generate_problems(data, renderer_name)` を呼び、成功時は `{'problems': [...]}` をそのまま JSON で返す(PDFファイルは生成しない)。`100` は単一の10×10加算表で `num` 個の同型 item のリストにならないため、`problems` list とは別の top-level `table` キーを使う(`num` は全 command 共通の入力検証のため必須のままだが `100` では意味を持たず無視される)。`command_type='ope'` 以外や `ope` の一部亜種フラグ、レンジが狭すぎる `100` など未対応・不正の入力は `problem_generation` 側が送出する `ValueError` を捕捉し `{'error': ...}` を HTTP 500 で返す(`/generate-pdf` と同じ「必須パラメータ欠如は400、ロジック層のエラーは500」という区分けを踏襲)。
- `GET /renderer-info`(issue #46): `renderers.get_renderer_name()` の結果をそのまま `{'renderer': 'latex'}` として返す(issue #186 以降、`latex` がデフォルトかつ唯一到達可能なレンダラーのため、通常は常に `'latex'`)。`nuts_calc.py` が `--vertical`(筆算)を持たなくなった(issue #46)ため、フロントエンドがリクエスト前にどちらのレンダラーが有効かを判定し、`latex` の場合のみ筆算 UI を出す目的で追加された(`frontend/web/src/catalog.js`・`preset.js`・`pcMakeFlow.js` が呼ぶ。かつては `frontend/spa/src/GradeDrills.jsx`/`CustomGenerator.jsx` も呼んでいたが、`frontend/spa` 自体が issue #233 で削除された)。env 変数が不正な場合は `renderers.get_renderer_name()` が送出する `ValueError` を捕捉し、`{'error': ...}` を HTTP 500 で返す。

## 統合ポイント

- `frontend/web/src/drillPresets.js` の `g5-gcd` は `command_type: 'gcd'` と `a_min`/`a_max`/`b_min`/`b_max` を送るため、issue #212 の内部 API 経路へ実際に到達する。
- `frontend/web/src/drillPresets.js` の「小学5年生」→「数の性質・九九」にある `g5-evenodd`(表示名「偶数・奇数」、`a_min: 1`/`a_max: 100`)と `g5-multiples`(表示名「倍数」、`a_min: 2`/`a_max: 12`)は、issue #214/#215 の内部 API 経路へ実際に到達する。
- `command_type == 'gcd'` は標準ライブラリ `math.gcd`、`nuts_calc_tex.generate_number_pair_problems`、`build_gcd_slot_content_tex`、および `problem_generation.resolve_digit_count_range` を呼ぶ(`app.py:265-328`)。
- 呼び出し元: `frontend/web/src/catalog.js`・`preset.js`・`pcMakeFlow.js`(`GET /renderer-info`)、`frontend/web/src/presetDetail.js`(`POST /generate-problems`、issue #139。`command_type: 'ope'` かつ未対応フラグなしのプリセットのみ、マウント時・設定変更時にデバウンスして呼び出す)。`frontend/web` には `com`/`pi` を使うプリセットも自由入力フォームも存在せず(`command_type_com`/`command_type_pi` という翻訳文字列だけが `frontend/web/src/strings.ja.json` に孤立して残っている)、`frontend/web` からは `command_type: 'com'`/`'pi'` いずれの `POST /generate-pdf` にも到達不可(issue #210 実装時に `frontend/web/src/drillPresets.js`/`drillCatalog.js` を確認して判明)。かつては `frontend/spa/src/CustomGenerator.jsx`(`frontend/spa/src/drillPresets.js` の「10のあわせて」`a_value: 10`/「100のあわせて」`a_value: 100` プリセットと、自由入力フォームの `<option value="com">` から到達)が `command_type: 'com'` を選択できる唯一のフロントエンドだったが、`frontend/spa` 自体が issue #233 で削除されたため、現状 `command_type: 'com'`/`'pi'` の `POST /generate-pdf` へ到達できる UI 経路は存在しない(直接 API を叩けば依然到達可能)。同様に `frontend/spa/src/GradeDrills.jsx` が呼んでいた `GET /renderer-info` の呼び出しも、`frontend/web` 側の上記3ファイルに引き継がれている。`command_type: '99'` は `com`/`pi` と異なり `frontend/web` から実際に到達可能: `drillPresets.js` の `g2-kuku` プリセット(「2年生」→「かけ算」→「九九」)が `dan`(段、`'1'`〜`'9'`)選択時に `command_type: '99'` の `POST /generate-pdf` を送信し、「出題順序」設定に応じて `descend`/`shuffle` も付与する(`dan === 'mixed'` の場合は `command_type: 'ope'` にフォールバックする別経路)。`command_type: 'squ'` は `frontend/web` の「平方数の計算」プリセット(`drillPresets.js` の `ungraded-squ`)から到達可能で、`command_type`/`a_value` のみを送信する(設定項目なし)。`command_type: 'lcm'` も `frontend/web` から到達可能: `drillPresets.js` の学年5「公倍数・最小公倍数」プリセット(`g5-lcm`)が `command_type: 'lcm'`/`a_min`/`a_max`/`b_min`/`b_max` を送信する(設定項目なし、`squ` と同型)。
- 呼び出し先: `command_type == 'com'` の場合は `backend/nuts_calc_tex.py` の内部プレゼンテーション API(`generate_com_problems`/`build_com_slot_content_tex`/`PresentationPage`/`ContentAreaLayout`/`DEFAULT_PAGE_SHELL`/`build_presentation_document_tex`/`get_latex_engine_adapter`、issue #199)を直接呼ぶほか、`backend/problem_generation.py`(`validate_com_target`、issue #237)を `a_value` 検証に呼ぶ。`command_type == 'lcm'` の場合(issue #211)は同じ内部プレゼンテーション API を標準ライブラリ `math.lcm` と `nuts_calc_tex.generate_number_pair_problems`/`build_lcm_slot_content_tex` で呼ぶほか、`problem_generation.resolve_digit_count_range`/`DEFAULT_A_MIN`/`DEFAULT_A_MAX`/`DEFAULT_B_MIN`/`DEFAULT_B_MAX` を `a`/`b` レンジの既定値解決に呼ぶ。`command_type == '99'` の場合は同じ内部プレゼンテーション API を `generate_kuku_problems`/`build_kuku_slot_content_tex`(issue #208)で呼ぶほか、`backend/problem_generation.py`(`validate_kuku_a_value`、issue #208)を `a_value` 検証に呼ぶ。`command_type == 'aBc'` の場合(issue #213)は `generate_abc_problems`/`build_abc_slot_content_tex` を使う。`command_type == 'pi'` の場合(issue #210)は同じ内部プレゼンテーション API を `generate_pi_problems`/`build_pi_slot_content_tex` で呼ぶほか、`backend/problem_generation.py`(`validate_pi_start`)を `a_value` 検証に呼ぶ(`validate_com_target` と対になる形で最初から共有バリデータとして抽出)。plain 2-term `ope` リクエスト(`_is_plain_ope_pdf_request` が真)の場合は同じ内部プレゼンテーション API を `generate_ope_problems`/`build_ope_slot_content_tex`(issue #205)で呼ぶほか、`backend/problem_generation.py` の `resolve_digit_count_range`/`DEFAULT_A_MIN`/`DEFAULT_A_MAX`/`DEFAULT_B_MIN`/`DEFAULT_B_MAX`/`DEFAULT_OPERATOR` を `a`/`b` レンジ・演算子の既定値解決に呼ぶ。`ope --use-parentheses` リクエスト(`_is_tree_ope_pdf_request` が真)の場合は同じ内部プレゼンテーション API を `generate_tree_ope_problems`/`build_tree_ope_slot_content_tex`(issue #206)で呼ぶほか、`problem_generation.resolve_digit_count_range` と `nuts_calc_tex.resolve_term_range`/`TERM_COUNT_FLOOR_DEFAULT` を `a`/`b` レンジ・`terms_min`/`terms_max` の解決に呼ぶ。かっこなし flat multi-term `ope` リクエスト(`_is_multi_term_ope_pdf_request` が真)の場合は同じ内部プレゼンテーション API を `generate_multi_term_ope_problems`/`build_multi_term_ope_slot_content_tex`(issue #207)で呼ぶほか、同じく `problem_generation.resolve_digit_count_range` と `nuts_calc_tex.resolve_term_range`(`use_parentheses=False`)を呼ぶ。`ope --missing-value` リクエスト(`_is_missing_value_ope_pdf_request` が真)の場合は同じ内部プレゼンテーション API を `generate_missing_value_problems`/`build_missing_value_slot_content_tex`(issue #223)で呼ぶほか、`problem_generation.resolve_digit_count_range`/`DEFAULT_A_MIN`/`DEFAULT_A_MAX`/`DEFAULT_B_MIN`/`DEFAULT_B_MAX`/`DEFAULT_OPERATOR` を `a`/`b` レンジ・演算子の既定値解決に呼ぶ(`carry`/`remainder`/`decimal` は虫食い算では未対応のため渡さない)。`ope --vertical` リクエスト(`_is_vertical_ope_pdf_request` が真)の場合は同じ内部プレゼンテーション API を `generate_ope_problems`/`build_vertical_ope_slot_content_tex`(issue #227)で `grid_layout='tabular'` を指定して呼ぶほか、`problem_generation.resolve_digit_count_range`/`DEFAULT_A_MIN`/`DEFAULT_A_MAX`/`DEFAULT_B_MIN`/`DEFAULT_B_MAX`/`DEFAULT_OPERATOR` を `a`/`b` レンジ・演算子の既定値解決に呼ぶ(`plain 2-term ope` と同じく `decimal`/`carry`/`remainder`/`result_max` も渡す。ただし `div` + 小数除数は `nuts_calc_tex.py:900-912` と同一メッセージで `ValueError` にする)。`ope --intermediate` リクエスト(`_is_intermediate_ope_pdf_request` が真)の場合は同じ内部プレゼンテーション API を `generate_ope_problems`(演算子は `['mul']` 固定)/`build_intermediate_ope_slot_content_tex`(issue #226)で呼ぶほか、`problem_generation.resolve_digit_count_range`/`DEFAULT_A_MIN`/`DEFAULT_A_MAX`/`DEFAULT_B_MIN`/`DEFAULT_B_MAX` を `a`/`b` レンジの既定値解決に、`nuts_calc_tex.INTERMEDIATE_SINGLE_DIGIT_MAX` を単一桁 `b` 検証に呼ぶ(`carry`/`remainder`/`decimal` は `mul` 限定 variant では無意味なため渡さない)。`command_type == 'squ'` の場合は同じ内部プレゼンテーション API を `generate_squ_problems`/`build_squ_slot_content_tex`(issue #209)で呼ぶほか、`backend/problem_generation.py`(`validate_squ_start`、issue #209)を `a_value` 検証に呼ぶ。それ以外は従来どおり `backend/renderers.py`(レンダラー選択・PDF生成実行の subprocess 経路)、`backend/problem_generation.py`(問題データのみのプロセス内直接生成)。
- issue #214/#215 の経路は `generate_evenodd_problems`/`build_evenodd_slot_content_tex` と `generate_multiples_problems`/`build_multiples_slot_content_tex` をそれぞれ直接呼び、共通の `PresentationPage`/`ContentAreaLayout`/`build_presentation_document_tex` へ渡す。
- issue #219〜#222・#225 の経路は `divfrac`/`simplify`/`frac2dec`/`dec2frac`/`commondenom` それぞれの生成関数と slot formatter を直接呼び、同じ `PresentationPage`/`ContentAreaLayout`/`build_presentation_document_tex` へ渡す(`app.py:265-327,1264-1588`)。
- issue #228 の `POST /generate-problems` `command_type == '100'` 経路は PDF/内部プレゼンテーション API を経由せず、`problem_generation.generate_hundred_square_table(data)`(内部で `nuts_calc_tex.generate_hundred_square` を呼ぶ)の戻り値をそのまま `jsonify` する。
- issue #229 の `POST /generate-pdf` `command_type == '100'` 経路(`_generate_hundred_square_pdf`)は `problem_generation.resolve_hundred_square_axes`(軸レンジ解決 + 最小値チェック、`generate_hundred_square_table` と共有)、`nuts_calc_tex.generate_hundred_square`、`nuts_calc_tex.build_hundred_square_slot_content_tex`、`PresentationPage`/`ContentAreaLayout(numbered=False)`/`build_presentation_document_tex`(`grid_layout='block'`)、`get_latex_engine_adapter` を呼ぶ。

## 注意事項・既知の制限

- `nuts_calc_tex.py` 側のバリデーション失敗メッセージは `print()`(stdout)に出力されるため、`subprocess.CalledProcessError` ハンドラは `e.stdout` を優先し(空なら `e.stderr` にフォールバック)、それを `error` フィールドに含める(issue #37 で修正)。`nuts_calc.py`(issue #232 で削除)側でも `com`/`99`/`squ`/`pi`/`100` のバリデーション失敗が引数なし `exit()`(終了コード0、`subprocess.run(check=True)` が例外を送出しない)になっていた不具合を同 issue #37 で `exit(1)` に修正済みだった。修正前はこの経路で `CalledProcessError` すら発生せず、後続の `send_file` が `FileNotFoundError` になり実際の理由と無関係な「Renderer script not found」を返していた。
- backend の URL がフロントエンド側にハードコードされている(`frontend/web/src/catalog.js`・`preset.js`・`pcMakeFlow.js`・`presetDetail.js` それぞれの `API_BASE = 'http://127.0.0.1:5000'` の既知の制約。かつて併存していた `frontend/spa/src/CustomGenerator.jsx`/`GradeDrills.jsx` も同様の制約を持っていたが、`frontend/spa` 自体が issue #233 で削除された)。
- レンダラー選択は env 変数のみで、リクエストごとの指定はできない(issue #36 のスコープ)。`GET /renderer-info` はこの env 変数由来のレンダラー名をフロントエンドに公開する読み取り専用エンドポイントであり、フロントエンドからレンダラーを切り替える手段ではない。
- `command_type == 'com'`/`'lcm'`/`'divfrac'`/`'gcd'`/`'evenodd'`/`'99'`/`'aBc'`/`'pi'`/`'100'`/`'squ'`/`'multiples'`/`'divisors'`/`'frac'`/`'simplify'`/`'frac2dec'`/`'dec2frac'`/`'compare'`/`'commondenom'`、基本2項 `mixed`、移行済み `ope` variants(`--missing-value`/`--intermediate` を含む)は、`nuts_calc_tex.get_latex_engine_adapter()` を各 `_generate_*_pdf` から直接呼ぶため、`NUTS_CALC_TEX_ENGINE` 環境変数が不正な場合の失敗経路が subprocess command と異なる。内部API経路では素の `ValueError` がルートで500へ変換される。
- 上記の内部 API command は常に1ページ・blank(練習用)版のみを返す。`with_bottom_answer`/`with_name_field`/複数ページ(`page` > 1)/`merge` は各 `_generate_*_pdf` に配線しておらず、リクエストに含めても無視される。`99`/`pi` は `descend`/`shuffle` のみ配線済みで、`reverse` は未配線である。`100` は `_generate_hundred_square_pdf` が常に blank 1表のみを返し、`show_answer`(filled 版)は未配線(legacy CLI の `--merge` 相当も含む)。CLI の `100`(`build_hundred_square_pages`/`--csv`)は無変更で残存する。
- `command_type == 'lcm'`/`'divfrac'`/`'gcd'`/`'evenodd'`/`'99'`/`'aBc'`/`'pi'`/`'squ'`/`'multiples'`/`'divisors'`/`'frac'`/`'simplify'`/`'frac2dec'`/`'dec2frac'`/`'compare'`/`'commondenom'` のルーティングは専用述語を介さず、`command_type` の単純一致で対応する `_generate_*_pdf` へディスパッチする。`ope` と異なり、これらには別のコンテンツフォーマットパターンを要求する移行済みバリアントがないため、分岐述語は不要である。`compare` は option flag(`comparison_pattern` 等)を持つが、issue #224 の scope が既定オプション限定で generator も非既定値を許容するため、`dec2frac` と同じ bare exact-match 分岐にした。
<<<<<<< HEAD
- plain 2-term `ope` の判定(`_is_plain_ope_pdf_request`)は `vertical`/`intermediate`/`use_parentheses`/`missing_value`/`terms`/`terms_min`/`terms_max`/`mixed_operators` のいずれかが指定されていれば内部API経路を使わず subprocess 経路にフォールバックする。`use_parentheses` は issue #206 で `_is_tree_ope_pdf_request` に切り出され、`vertical`/`intermediate`/`missing_value` のいずれも併用していなければ内部API経路(`_generate_tree_ope_pdf`)へルーティングされるようになった(`terms`系/`mixed_operators` は tree variant 自身の N項一般化として許容)。`terms`系(`use_parentheses` 非併用時)も issue #207 で `_is_multi_term_ope_pdf_request` に切り出され、`vertical`/`intermediate`/`use_parentheses`/`missing_value` のいずれも併用していなければ内部API経路(`_generate_multi_term_ope_pdf`)へルーティングされるようになった。`missing_value` も issue #223 で `_is_missing_value_ope_pdf_request` に切り出され、`vertical`/`intermediate`/`use_parentheses`/`mixed_operators`/`terms`系のいずれも併用していなければ内部API経路(`_generate_missing_value_ope_pdf`)へルーティングされるようになった(パターン2)。`vertical` も issue #227 で `_is_vertical_ope_pdf_request` に切り出され、`intermediate`/`use_parentheses`/`missing_value`/`mixed_operators`/`terms`系のいずれも併用していなければ内部API経路(`_generate_vertical_ope_pdf`、`grid_layout='tabular'`)へルーティングされるようになった(パターン6)。残るのは `intermediate`(`docs/latex/tex_content_format_taxonomy.md` 上で別のコンテンツフォーマットパターン5)のみで、これは依然として subprocess 経路のまま。

## 変更履歴（git log より自動生成）

- c9e6de7 feat(#227): migrate ope --vertical to the internal presentation API
=======
- plain 2-term `ope` の判定(`_is_plain_ope_pdf_request`)は `vertical`/`intermediate`/`use_parentheses`/`missing_value`/`terms`/`terms_min`/`terms_max`/`mixed_operators` のいずれかが指定されていれば内部API経路を使わず subprocess 経路にフォールバックする。`use_parentheses` は issue #206 で `_is_tree_ope_pdf_request` に切り出され、`vertical`/`intermediate`/`missing_value` のいずれも併用していなければ内部API経路(`_generate_tree_ope_pdf`)へルーティングされるようになった(`terms`系/`mixed_operators` は tree variant 自身の N項一般化として許容)。`terms`系(`use_parentheses` 非併用時)も issue #207 で `_is_multi_term_ope_pdf_request` に切り出され、`vertical`/`intermediate`/`use_parentheses`/`missing_value` のいずれも併用していなければ内部API経路(`_generate_multi_term_ope_pdf`)へルーティングされるようになった。`missing_value` も issue #223 で `_is_missing_value_ope_pdf_request` に切り出され、`vertical`/`intermediate`/`use_parentheses`/`mixed_operators`/`terms`系のいずれも併用していなければ内部API経路(`_generate_missing_value_ope_pdf`)へルーティングされるようになった(パターン2)。`intermediate` も issue #226 で `_is_intermediate_ope_pdf_request` に切り出され、`vertical`/`use_parentheses`/`missing_value`/`mixed_operators`/`terms`系のいずれも併用していなければ内部API経路(`_generate_intermediate_ope_pdf`、パターン5、`mul` のみ・単一桁 `b` の基本ケース)へルーティングされるようになった。残るのは `vertical`(`docs/latex/tex_content_format_taxonomy.md` 上で別のコンテンツフォーマットパターン6)のみで、これは依然として subprocess 経路のまま。

## 変更履歴（git log より自動生成）

- 8c28b2e feat(#226): migrate ope --intermediate to the internal presentation API
>>>>>>> origin/main
- 2d4c435 feat(#225): migrate commondenom to the internal presentation API (#274)
- 84c789b feat(#224): migrate compare to the internal presentation API (#273)
- 7943190 feat(#223): migrate ope --missing-value to the internal presentation API (#272)
- 7585ce7 feat(#229): migrate the 100 hundred-square command to the internal presentation API (#271)
- c952709 feat(#228): expose the 100 hundred-square table via the /generate-problems JSON contract (#262)
- ce8f8b6 feat(#222): migrate dec2frac to the internal presentation API (#261)
- 4cb1c11 feat(#221): migrate frac2dec to presentation API
- 156c2d2 Merge remote-tracking branch 'origin/main' into feat/220-migrate-simplify-presentation-api
- ab8daf7 feat(#220): migrate simplify PDF generation
